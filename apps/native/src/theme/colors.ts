/**
 * Centralized surface colors for native app.
 * Use these for screen backgrounds, accordion sections, cards, tiles, and ads
 * so styling stays consistent and can be updated in one place.
 */

/** Surface background for screens, cards, accordion sections, tiles. Matches ads container. */
// Derived from Tamagui dark Button background: hsla(0, 0%, 14%, 1) ≈ #242424
export const SURFACE_BACKGROUND = "#242424";

/** Surface border. Matches ads border and dividers. */
// Derived from Tamagui dark Button borderColor: hsla(0, 0%, 27%, 1) ≈ #454545
export const SURFACE_BORDER = "#454545";

/** Surface background at 45% opacity (for ads container). */
export const SURFACE_BACKGROUND_SEMI = "rgba(36, 36, 36, 0.45)";
