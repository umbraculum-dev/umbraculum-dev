# Local DB backups (pg_dump)
backups/
# ------------------------------------------------------------
# Local-only / developer-specific (do not commit)
# ------------------------------------------------------------

# Local development notes (repo-specific)
DEVELOPMENT.md

# Cursor artifacts (synced via npx / developer-managed)
.cursor/
.cursorignore

# npm auth / registry config (often contains tokens)
.npmrc

# ------------------------------------------------------------
# Secrets / environment
# ------------------------------------------------------------
.env
.env.*
!.env.example

# ------------------------------------------------------------
# Node / Next.js / TypeScript
# ------------------------------------------------------------
node_modules/
.next/
out/
dist/
build/
coverage/
.turbo/
.eslintcache
*.tsbuildinfo

# npm/yarn/pnpm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ------------------------------------------------------------
# Playwright (when added)
# ------------------------------------------------------------
playwright-report/
test-results/

# ------------------------------------------------------------
# Prisma / SQLite dev DBs (when added)
# ------------------------------------------------------------
**/*.db
**/*.db-journal

# ------------------------------------------------------------
# Docker (optional local overrides)
# ------------------------------------------------------------
docker-compose.override.yml
docker-compose.*.override.yml

# ------------------------------------------------------------
# Local scratch / runtime artifacts
# ------------------------------------------------------------
.local/
tmp/
var/
logs/
*.log

# ------------------------------------------------------------
# IDE / OS
# ------------------------------------------------------------
.vscode/
.idea/
.DS_Store

