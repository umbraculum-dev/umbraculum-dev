/**
 * Brewery-vertical locale bundles for web and native apps.
 * Consumed by @umbraculum/i18n getSharedMessages() merge (reference vertical).
 */

import enData from "./en.json";
import itData from "./it.json";

const en = enData as Record<string, unknown>;
const it = itData as Record<string, unknown>;

export { en, it };
