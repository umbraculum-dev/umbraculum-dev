# @umbraculum/api is an npm workspace member — versions are locked at the repo root
# (package-lock.json). The api compose service runs `npm install` with cwd=/app; without
# this file npm writes an ephemeral services/api/package-lock.json onto the bind mount.
package-lock=false
