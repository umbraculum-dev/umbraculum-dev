generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal baseline model just to validate migrations end-to-end.
model HealthCheck {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
}

