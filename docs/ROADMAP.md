# Roadmap (living)

**Tier:** Public

This roadmap captures the agreed “direction of travel” for the product so implementation stays coherent and we avoid rework.

## Big picture

- Platform vision (horizontal-platform-with-vertical-modules + AI consultant + add-on pricing): `docs/PLATFORM-ARCHITECTURE.md`
- Licensing posture and rationale (AGPLv3 core + MIT SDK + commercial dual license): `docs/LICENSING.md`
- Source of truth (brewery-vertical implementation log): `docs/architecture-Rev02.md`
- Accessibility hard constraint: `docs/DEVELOPMENT-ACCESSIBILITY.md`
- Seed data sources + licensing notes: `docs/RAW-MATERIALS-SEEDABLE-SOURCES.md`
- Full doc index: `docs/README.md`

## Trajectory (12–30 months)

This is the agreed direction of travel at the platform level. It is intentionally short on implementation detail and long on sequencing — implementation specifics land in domain docs and per-quarter planning. The trajectory exists to keep parallel work coherent and to make explicit which decisions need to happen *before* the second vertical lands.

### H2 2026 — AI consultant as cross-module connective tissue

Goal: ship the smallest AI consultant that demonstrably provides value across the brewery vertical, with the architecture intentionally platform-shaped from day one (not brewery-shaped with platform aspirations).

- **Architecture**: AI platform backbone per `docs/PLATFORM-ARCHITECTURE.md` §4.3 and §5.3 — orchestrator, brewery tool registry, usage ledger, encrypted BYOK settings, workspace memory, Anthropic-only v0 provider path, and future-ready seams for managed AI.
- **Tools layer first** (`Layer A`): roughly 80% of value, lowest risk. Read-only, ACL-aware, deterministic. Brewery tools are the v0 surface.
- **Per-workspace operational memory store** stood up alongside the tool layer. This is the moat investment (`docs/PLATFORM-ARCHITECTURE.md` §4.3, §6.5) — the AI's recall of seasonal patterns, supplier quirks, and recurring failure modes compounds over time and is not transferable to a competitor.
- **Write-action drafts with human-in-the-loop confirmation** from the first AI feature that touches mutable state. No autonomous writes in v0 or v1.
- **License + governance** as a parallel track: publish `docs/LICENSING.md` publicly, adopt DCO sign-off on contributions, write the contributor README, and pick the AGPLv3 + MIT SDK split intentionally before the first community contribution arrives (much harder to retrofit after).

Resolved in this phase (recorded in `docs/PLATFORM-ARCHITECTURE.md` §8): BYOK + paid tier unlock, Anthropic-only v0, opt-in workspace enablement, no per-user role gate, water + recipe coach scope, per-workspace memory, and no net-new Stripe surface for v0 AI.

### H1 2027 — Brewery production planning is promoted to first-class MRP/CRP, and the platform is repositioned

Goal: realize the §1.1 reframe in code and in market positioning. Same product, much larger addressable market.

- Generalize the brewery production-planning subsystem (recipes-as-BOMs, equipment profiles as constrained resources, brew sessions as scheduled production orders) into platform-level MRP and CRP capabilities. The brewery vertical becomes a configuration of these capabilities rather than a private implementation.
- AI consultant demonstrably reasons across recipe planning + production scheduling + capacity load.
- Platform repositioned externally: "process-manufacturing platform, brewery-configured by default". Brand and copy reflect this on public surfaces (marketing site, documentation, App Store listings).
- `@brewery/*` horizontal packages renamed to a neutral platform scope (`docs/PLATFORM-ARCHITECTURE.md` §3.3, §5.2). This is the one-way structural move that should not be deferred past this phase.
- Module SDK published as a public artifact (`docs/PLATFORM-ARCHITECTURE.md` §4.4). Third-party module developers can build modules in their own repositories.
- `WorkspaceBillingAddon` model + Stripe subscription-item flow + RevenueCat consumables shipped — the prerequisite for selling per-module entitlements and optional managed-AI credits cleanly.
- **Foundation hardening pass (lint, types, tests) — ESLint slice landed in May 2026, ~12 months ahead of the H1 2027 milestone.** Both **HIGH-staged ESLint** (`no-explicit-any` + `no-unused-vars` to `error` repo-wide; warning-free monorepo under the current rule set) and **HIGH-full** (12 type-aware rules at `error` — 7 promise-correctness rules `**/*.{ts,tsx}` + 5 `no-unsafe-*` rules across `services/api/**`, `apps/web/**`, and `apps/native/**`; `no-empty-object-type: error`; `--max-warnings 0` is the single CI gate) completed on 2026-05-16 in a single intensive day across 25 commits. The originally-feared "1,671 type-aware warnings, Tamagui wall, 44s lint cost" measurement decomposed into much smaller real fixes once it was investigated: the apps/web "Tamagui wall" was a phantom (95% of the 290+ apps/web `no-unsafe-*` warnings traced to a single stale `account → workspace` field rename in commit `87876d0` that missed four UI consumers — fixing the lint also restored a long-broken UI feature where workspace water profiles silently never appeared in dropdowns); the apps/native surface was 3 warnings in 1 file; the services/api surface was real but tractable Prisma + Fastify + AI-tool boundary tightening. The IDE-cost objection was avoided by structural design — the editor-config split (`eslint.config.editor.mjs` + `.vscode/settings.json.example` + the upstream `23a-eslint-fixall-discipline.mdc` cursor rule) keeps editor inline lint at ~7s wall (essentially unchanged) while CI gets the full ~42s type-aware pass. Phase 7 (the runtime-validation library question — Zod / Valibot / TypeBox) was tracked separately in [`docs/CONTRACTS-VALIDATION-STRATEGY.md`](CONTRACTS-VALIDATION-STRATEGY.md) with explicit per-criterion trigger conditions; first scheduled audit on 2026-05-16 confirmed 0/6 criteria met → "stay on hand-rolled" re-confirmed. **Tests slice (cheapest-test-layer coverage gaps) kicked off 2026-05-17** — Phase 1 (L1 contract parser coverage) landed the same day, bringing the contract-parser test count from 5/8 to 8/8 (70 unit tests across 5 suites). Phase 2 (L4 BeerJSON contract snapshots) **complete 2026-05-18, all three sub-phases landed in one day** — `/water-profiles` keystone Phase 4b regression-pin, the full water-calc surface (`/water-settings`, `/water-hub-summary`, mash/sparge/boil `compute-and-save`), brew-sessions (POST create, GET list, GET detail), equipment-profiles, and ingredients (fermentables/hops/yeasts). L4 coverage 2/15 → 15/15 native-consumed endpoints; 7 contract suites with 16 tests total. **Phase 3 (L5 Playwright regression pins for Phase 4b + Phase 5g) landed 2026-05-18.** Phase 3a: `apps/web/e2e/smoke/water-profiles.spec.ts` asserts the seeded "E2E Tap Water" workspace profile appears in the `/en/water-profiles` table + asserts `/api/water-profiles` carries `body.workspace` (not `.account`); 2 tests. Phase 3b: `apps/web/e2e/smoke/select-workspace.spec.ts` asserts the multi-workspace login redirect, the picker UI shows both seeded workspaces, and `POST /api/auth/active-workspace` mutates the session correctly; 3 tests; required adding the `e2e-multi-admin` persona + secondary workspace `E2E Side Brewery` (without which the SelectWorkspace route was a dead E2E branch). Smoke suite: 13 → 18 specs, all green. **The Phase 4b regression-pin trio is now complete across L1 + L4 + L5; the Phase 5g L5 web pin is complete (apps/native side deferred until apps/native gets a test runner — tracked in TESTING.md non-goals).** **Phase 4a (L2 route surface audit) landed 2026-05-18** — inventoried ~120 routes across 28 route files against 18 L2 test files, surfaced highest-signal gap (cross-workspace isolation on the 17-route brew-sessions surface; 3 workspace-scoped route files with zero L2 coverage: inventory, brewday-settings, integrations-generic-non-tilt), and scoped 5 implementation sub-phases (Phase 4b-1 through 4b-5). Documented the architectural finding that `AclService.requireRole` exists but is unwired from routes (known v0 state). **Phase 4b-1 (brew-sessions cross-workspace isolation) landed 2026-05-18** — 7 new tests in `brewSessions.test.ts` covering the 6 highest-risk routes (GET detail + PATCH date + DELETE + start/pause/stop) with a second persona/workspace expecting 404; closes the audit's highest-signal L2 gap. **Phase 4b-2 (inventory L2 from scratch) landed 2026-05-18** — new `services/api/src/tests/inventory.test.ts` (20 tests) covers all 4 inventory routes (GET list + POST + PATCH + DELETE) across 5 axes (happy + ?category= filter + category-conditional metadata round-trip; validation 400s for invalid_category / invalid_unit / invalid_name / invalid_quantity; auth 401s with both `missing_session` and `missing_active_workspace` flavors; cross-workspace 404 + positive "B's list does not contain A's items" check; pinned Postgres enum-declaration-order sort behavior); closes the audit's largest zero-coverage workspace-scoped writable surface. **Phase 4b-3 + 4b-4 (paired) landed 2026-05-18** — new `services/api/src/tests/brewdaySettings.test.ts` (9 tests, 2 routes; happy / PATCH round-trip / auth / cross-workspace) + auth-pin sibling `describe` block appended to `services/api/src/tests/recipeWaterSettings.test.ts` (8 tests, 4 routes × 2 unauth flavors for the 3 compute-and-save + 1 hub-summary endpoints that previously had L4-only coverage; pins the `requireActiveWorkspace` gate explicitly so a future regression removing it surfaces here even though L4 would still pass). **Phase 4b-5 (platform-admin gate) landed 2026-05-18** — new `services/api/src/tests/platformAdminRoutes.test.ts` (27 tests) covers all 12 platform-admin routes (`platformAds.ts` 4 + `platformRecipes.ts` 8) with 12 × 401 `missing_session` + 12 × 403 `not_platform_admin` + 3 admin happy-paths (GET /platform/ads list shape, GET /platform/workspaces cross-workspace visibility, POST /platform/ads create with DB round-trip). Admin user is provisioned via direct `app.prisma.user.update({ isPlatformAdmin: true })` after standard helper-based setup (helper deliberately doesn't accept the flag — keeps unrelated tests from accidentally promoting users). Services/api total: 162 → 233 tests across 40 files (+71 tests / +4 new files / +1 extended in 2 days), all green. **Phase 4 is now complete — all 5 Phase 4b gap-fix sub-phases have shipped on 2026-05-18.** **Phase 5a (`packages/core` L1 audit + gap-fix) landed 2026-05-18** — all function exports across the 4 source files (`gravity.js`, `units/units.js`, `units/rounding.js`, `water.js`) already covered with reasonable depth; gap-fix added a new `packages/core/src/water.test.js` pinning the previously-untested `DEFAULT_MASH_TARGET_PH` constant (documented as coupled to the Prisma column default — silent-drift risk), plus extensions to the 3 existing test files pinning 6 behavioral contracts. packages/core test count: 26 → 47 across 3 → 4 files. The audit also surfaced **Phase 5b**: `services/api/src/domain/waterCalc/*` has ~1000 LoC of pure math/derivation code across 4 substantial + 5 smaller files with no direct L1 unit tests — only INDIRECT L4 snapshot coverage. Split into 5 sub-phases (5b-1 → 5b-5). **Phase 5b-1 (stoichiometry + alkalinity primitives) landed 2026-05-18** — new `services/api/src/domain/waterCalc/saltAdditions.test.ts` (24 tests) pins per-salt stoichiometry against Bru'n-Water-convention reference numerics for all 5 supported salts (gypsum, CaCl2·2H2O, epsom, NaCl, NaHCO3) at `toBeCloseTo(value, 1)` precision = 0.1 ppm, plus linearity / inverse volume scaling / multi-salt accumulation / breakdown shape invariants / all 9 validation error paths. New `residualAlkalinity.test.ts` (17 tests) pins the 0.713 Ca + 0.588 Mg Kolbach coefficients, the clamp-to-zero on effective alkalinity, defaults-to-zero for missing Ca/Mg, all non-finite throw paths, and the symmetry between the standalone helper and `effectiveAlkalinityPpmCaCO3FromCaMg`'s embedded calculation. **🎉 Phase 5b-5 (overall + mashPhEstimate wrapper) landed 2026-05-18 — the test-coverage hardening slice is now COMPLETE for all non-deferred phases.** New `services/api/src/domain/waterCalc/overall.test.ts` (20 tests) pins the bicarbonate ↔ alkalinity unit-conversion factors (50/61 ≈ 0.8197 and its reverse 61/50 ≈ 1.22) including round-trip symmetry (alk → bicarb → alk returns the original value for a sweep of {0, 1, 50, 100, 250.5}); the `deriveBicarbonatePpmFromAlkalinityPpmCaCO3` clamp-to-zero contract (brewing reporting constraint: "reported bicarbonate concentration cannot be negative" — matters because the acid step can drive `finalAlkalinityPpmCaCO3` below zero mathematically); the `combineAfterSaltsAndAcid` composition contract (Ca/Mg/Na come through unchanged from afterSalts; sulfate + chloride get the acid-step deltas ADDED; **bicarbonate is OVERWRITTEN by acidResult.finalAlkalinityPpmCaCO3, NOT propagated from afterSalts.bicarbonate** — the most subtle invariant; the acid step is the final source of truth for alkalinity); and the saltAdditions → acid step bridge `alkalinityAfterSaltsPpmCaCO3FromSaltAdditionsResult`. New `mashPhEstimate.test.ts` (3 tests) pins the 16-LoC wrapper-forwards-verbatim contract via deep-equal comparison with `mashPhEstimateV1` (realistic input + empty-grist input) and identical error propagation. services/api test count: 233 → 400 across 40 → 49 files (+167 tests / +9 new files across the entire test-coverage hardening pass; the project was at 162 tests when the pass started on 2026-05-17). **Every non-trivial file in `services/api/src/domain/waterCalc/` (9 of 9) now has direct L1 unit tests.** Only Phase 4d (deferred role-based ACL coverage) remains, gated on the ACL-wiring architectural decision. The audit explicitly traces the two latent UI bugs the lint slice surfaced (Phase 4b `account → workspace` stale-consumer drift; Phase 5g untyped render-prop) to the test layers that should have caught them (L4 BeerJSON contract snapshots for Phase 4b; L5 Playwright spec for Phase 5g). See [`docs/TESTING.md`](TESTING.md) → "Coverage audit + hardening pass" + "Phase 5a `packages/core` audit + gap-fix" for the full phase log. **Types slice Phases 1 + 2 + 3a + 3b + 4 + 5 + 6a + 6b + 6c + 6e + 6f + 6g (audit + baseline + SoT doc + methodology correction + full services/api remediation + apps/web typecheck infrastructure + Tamagui accept-vs-fix decision + per-workspace CI typecheck gate for the 13 clean workspaces + 3 partial strict-flag rollouts + 1 full repo-wide rollout of `noPropertyAccessFromIndexSignature` fixing 2566 sites across 8 workspaces in one session + 1 in-gate full rollout of `noUncheckedIndexedAccess` to services/api + packages/contracts fixing 25 TS2532 sites across 6 files + 1 in-gate full rollout of `exactOptionalPropertyTypes` to the 5 deferred workspaces fixing 73 TS2375/2379/2322/2412 sites via type-widening + conditional-spread + non-null-assertion patterns; **all 13 CI-gated workspaces now carry all 4 candidate strict flags**) landed 2026-05-18** — new `docs/TYPING.md` is the third sibling SoT to `docs/TESTING.md` + `docs/LINTING.md`. **Phase 1** (commit `6f3c2cc`) published the initial v0.1 audit; **Phase 2 (same day)** corrected the measurement methodology after discovering that v0.1's baselines mixed three different `node_modules` resolution trees (long-running `web` container → `/packages/node_modules` symlink to apps/web's deps, which doesn't have `vitest`; long-running `api` container with locally-generated Prisma client; one-off `node:20-slim` container with monorepo-root hoisted `/repo/node_modules`). v0.2 standardizes on the one-off-container method (same as the existing `apps/native` CI gate via `.github/workflows/native-deps.yml`), which is reproducible and matches CI conditions. Corrected v0.2 baseline across all 15 workspaces (14 TS + the JS-only `packages/core`): **12 are clean** (`apps/native`, `apps/web/e2e`, `services/api/prisma`, `packages/api-client`, `packages/beerjson`, `packages/contracts`, `packages/i18n`, `packages/i18n-react`, `packages/media`, `packages/navigation`, `packages/recipes-ui`, `packages/test-mcp`); **3 currently fail on `main`** with **629 latent type errors total** (`apps/web` 585 — Tamagui shorthand props per the accepted-cost position in [`docs/TAMAGUI.md`](TAMAGUI.md); the canonical count is within 1% of the 590 documented there, so TAMAGUI.md does not need updating; `packages/ui` 25 — same Tamagui class; `services/api` 19 — Prisma `Json` column typing on `metadataJson` in `brewdaySettingsService.ts` + `inventoryService.ts`, `UnitConversionWarningV1` shape mismatch in `recipesImportService.ts`, Prisma `Where` clause readonly-tuple unification in `routes/ingredients.ts`, Prisma client de-narrowing in a `brewSessionsService.ts:924` transaction lambda, and 4 type errors in tests added during the test-coverage hardening pass that vitest didn't catch because esbuild transpiles without type-checking — useful side-finding pinning that **the L1+L2 test layers are not a substitute for `tsc --noEmit`**). The originally-planned Phase 2 ("add `vitest` to `packages/beerjson` + `packages/contracts` tsconfig types") was **CANCELLED** — those errors were v0.1 measurement artifacts, not real type debt. These failing baselines are NOT a regression introduced by the audit — they're latent debt that has never been gated in CI; today only `apps/native` runs `tsc --noEmit` as a CI gate. The audit also piloted 4 candidate stricter flags inline across 8 clean workspaces to size the rollout cost: `noUncheckedIndexedAccess` produces only **5 new errors total** (all in `apps/native` — 2 in `FermDataIntegrationScreen.tsx` + 3 in `WaterMashScreen.tsx`, all real latent index-out-of-bounds bugs); `exactOptionalPropertyTypes` produces **12 new errors** (apps/native 6, api-client 3, recipes-ui 3 — dominant friction is `Partial<T>` call sites with explicit-`undefined`); `noPropertyAccessFromIndexSignature` and `noImplicitOverride` produce **0 new errors** across all 8 measured clean workspaces. Bounded sub-phase plan published in TYPING.md: **Phase 3a (✅ landed 2026-05-18) fixed 5 of the 19 services/api errors** — the type errors I authored in tests during the test-coverage hardening pass (Phases 5b/5b-5) that vitest didn't catch because esbuild's transpile-only mode skips type-checking. Fixes: (i) `gravityAnalysis.test.ts` — extracted a `TestHopInput` type alias so `defaultHop` and `args.hops` share a shape, fixing the union that dropped the `form?` property; (ii) `gravityAnalysis.ts:548` — production code on the same path was missing `buGuRatio: null` from the early-return `GravityAnalysis` literal (the other GravityAnalysis construction site on line 751 already had it); (iii) `overall.test.ts:197+213` — added the required `baseProfile: IonProfilePpm` field to two `SaltAdditionsResult` mocks. services/api dropped from 19 → 14 errors. All 400 services/api vitest tests still pass — this is a pure type-shape correction with no runtime behavior change. **This commit also pins the corollary finding from Phase 2** that the L1+L2 test layers are not a substitute for `tsc --noEmit`: vitest tests stayed green throughout the whole 5b/5b-5 hardening pass while accumulating these type errors. Phase 3b (next) fixes the 14 remaining production-code errors (Prisma `Json` column typing on `metadataJson` in `brewdaySettingsService.ts` + `inventoryService.ts`; `UnitConversionWarningV1` shape mismatch in `recipesImportService.ts`; Prisma `Where` clause readonly-tuple unification in `routes/ingredients.ts`; Prisma client de-narrowing in 4 `brewSessionsService.ts` transaction lambda sites). **Phase 3b (✅ landed 2026-05-18) fixed the remaining 14 services/api production-code errors across 4 issue classes** — (A) Prisma `Json` column typing: added `Prisma.InputJsonValue` casts on `sectionsJson`/`customSectionsJson`/`defaultStepsJson` in `brewdaySettingsService.ts` and on `metadataJson` (create + update paths) in `inventoryService.ts`; update path maps JS `null` → `Prisma.DbNull` to preserve "clear to SQL NULL" runtime semantics. (B) Prisma `Where` clause unification: refactored fermentables/hops/yeasts routes in `routes/ingredients.ts` from spread-based `as const` literals to explicit `AND: Prisma.<Entity>WhereInput[]` composition. **The refactor also fixed a latent cross-workspace data-leak bug**: the original spread silently overwrote the workspace-scoping `OR` with the search-query `OR` when both `activeWorkspaceId` AND `q` were present — exposing all workspaces' rows to search. Affected all three routes (`/ingredients/fermentables`, `/ingredients/hops`, `/ingredients/yeasts`). This is exactly the bug class L1+L2 tests don't catch (they test a single workspace's listing) and ESLint can't see (correctness, not code-smell) — a high-value type-hardening dividend, documented as Side-finding 2 in TYPING.md. (C) Transaction-lambda de-narrowing: `addStepTimerDeltaSeconds.tx` parameter retyped from `PrismaClient` to `Prisma.TransactionClient` to accept the `Omit<PrismaClient, "$on"|"$connect"|"$disconnect"|"$transaction"|"$extends">` shape that `prisma.$transaction(async (tx) => …)` provides. (D) `UnitConversionWarningV1` shape: replaced 2 lying `as Array<{ code; message }>` casts in `recipesImportService.ts` with real `.map(w => ({ code, message: \`${path}: ${fromUnit} → ${toUnit}\` }))` projections that produce user-facing messages. services/api dropped from 14 → **0 errors**; all 400 services/api vitest tests still pass. **Monorepo total: 624 → 610 errors, all 610 in the Tamagui-accepted class (apps/web 585, packages/ui 25) per [`docs/TAMAGUI.md`](TAMAGUI.md). 13 of 15 workspaces are now clean.** **Phase 4 (✅ landed 2026-05-18) wired the apps/web typecheck infrastructure**: added `"typecheck": "tsc -p tsconfig.json --noEmit"` to `apps/web/package.json`; verified `npm run typecheck` reproduces the canonical 585 baseline; pinned the explicit apps/web accept-vs-fix decision: apps/web stays OUT of the Phase 5 CI typecheck gate (the 585 errors are all Tamagui-class noise per TAMAGUI.md; gating them produces no signal, locks in the noise count, and harms future Tamagui upgrades). TAMAGUI.md reports 590 errors; canonical 585; 0.85% drift, within TAMAGUI.md's own 1% tolerance, so TAMAGUI.md needs no update this cycle. Revisit triggers documented (Tamagui upstream typing improvements; non-Tamagui error class accumulating inside the noise; total count crossing TAMAGUI.md's 1000-in-single-app threshold). **Phase 5 (✅ landed 2026-05-18) added `.github/workflows/typecheck.yml`** — single-job CI workflow that runs `tsc --noEmit` across the 13 clean workspaces inside one `node:20-slim` container (the canonical measurement method documented in TYPING.md §Methodology). Sequential per-workspace invocation under `set +e` with explicit pass/fail aggregation so all 13 results are reported even on early failure. Gated set: `services/api`, `services/api/prisma`, `apps/native`, `apps/web/e2e`, `packages/api-client`, `packages/beerjson`, `packages/contracts`, `packages/i18n`, `packages/i18n-react`, `packages/media`, `packages/navigation`, `packages/recipes-ui`, `packages/test-mcp`. apps/web + packages/ui excluded per Phase 4 decision. `apps/web/e2e` previously lacked a `typecheck` npm script — added in this phase so all 12 npm-workspace targets use uniform `npm run typecheck`. Local simulation green, ~33s wall + ~30s `npm install` = ~63s CI total (well under the 15-min timeout). **The types slice now has the same regression-guard discipline as the lint slice (`web-lint.yml`) and the test slice (`api.yml`).** **Phase 6a (✅ landed 2026-05-18) enabled `noImplicitOverride` in all 14 leaf tsconfigs (universal; corrected canonical measurement: 0 cost everywhere) + `noPropertyAccessFromIndexSignature` in the 7 truly-free workspaces (packages/api-client, packages/i18n, packages/i18n-react, packages/media, packages/navigation, packages/recipes-ui, packages/ui).** Side-finding: the Phase 1 pilot data was inaccurate for 3 of 4 candidate flags — the pilot sampled only 8 small clean workspaces (none of which had index-signature-heavy types) and extrapolated to the full repo. Attempting Phase 6a "free flag" rollout surfaced 2566 hidden TS4111 errors (services/api 1388 from Fastify request envelope index signatures, packages/contracts 472 from Prisma dynamic dispatch, apps/web 351 from Next.js dynamic routes + `process.env`). Corrected exhaustive per-flag-per-workspace baseline now published in TYPING.md §"Candidate stricter flags — corrected canonical baseline". Lesson: type-system errors don't distribute uniformly — they cluster around specific patterns; sample exhaustively, not representatively. Phases 6b/6c (next) enable `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` in their respective truly-free workspace sets (9 + 6 workspaces). Phases 6e/6f/6g defer remediation of the non-free workspaces. **Phase 6f (✅ landed 2026-05-18) enabled `noUncheckedIndexedAccess` in services/api + packages/contracts — the last 2 of the 13 CI-gated workspaces still missing the flag.** Fixed 36 TS2532 sites across 9 files. Production (3 files, 5 patch sites, 11 errors): services/api/src/routes/auth.ts (2 sites — `memberships[0]?.id ?? null`); services/api/src/services/ai/keyVault.ts (1 site — destructure cast `as [string, string, string]` after the `parts.length !== 3` throw); services/api/src/services/recipesImportService.ts (2 sites — extract `const m = found[0]!` after `length === 1`). Tests (6 files, 25 errors): services/api ai integration tests + keyVault crypto tests; packages/contracts meResponse + waterProfile + parseHubSummary + parseComputeAndSave — all use `arr[0]!` non-null assertion where surrounding test logic (`toHaveLength(1)` etc.) proves definedness. All 400 services/api + 70 contracts vitest tests still pass; flag enabled in both tsconfigs; CI gate continues to enforce. **All 13 CI-gated workspaces now carry `noUncheckedIndexedAccess`.** **Phase 6g (✅ landed 2026-05-18) enabled `exactOptionalPropertyTypes` in the 5 deferred workspaces (apps/native, packages/beerjson, packages/recipes-ui, packages/contracts, services/api).** Fixed 73 TS2375/TS2379/TS2322/TS2412 sites using three consistent patterns: (a) **type-widening** at the type-declaration site — added `| undefined` to optional fields on editor types in `@brewery/beerjson` (EditorGristRow, EditorHopRow, EditorYeastRow, EditorMash, EditorMashStep, EditorMiscRow), public response contracts in `@brewery/contracts` (AuthMeResponseUser/Workspace, NumberFormatHintV1, WaterCalcDerivation, WaterAcidification/OverallResult, WaterProfile, the 3 ComputeAndSave response types, RecipeWaterHubSummaryResponse), and ~25 internal services/api input types (MashPhEstimateV1Input, SpargeAcidification/MashAcidification family, UpsertRecipeWaterSettingsInput at ~100 fields scripted via a small Python helper, plus various Service `UpdateXxxInput`s and `MashAcidificationTargetMashPhGristRow`); (b) **conditional spread** at 7 Prisma `create`/`update` call sites (`...(value !== undefined ? { field: value } : {})`) in recipesService.ts + brewdaySettingsService.ts + integrationsService.ts ×2 + workspaceBillingService.ts ×2 + billingIntentsService.ts + waterProfilesService.ts + beerproto.ts ×2 — Prisma's generated types are 3rd-party-owned and shouldn't be widened; (c) **non-null assertion** at 6 `argv[++i]!` sites in services/api/src/cli/{restoreEquipmentProfileFromRecipe.ts, seed-import.ts} where the surrounding `parseArgs` validation already guarantees the value exists. Two intermediate `tsup` rebuilds of beerjson + contracts were needed mid-rollout because consumers resolve those packages via `dist/` not `src/`. **All 13 CI-gated workspaces now carry all 4 candidate strict flags** (`noImplicitOverride`, `noPropertyAccessFromIndexSignature`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`). Side-finding 5 (the `Partial<T>` exactOptional gotcha — TS team explicitly chose not to fix in microsoft/TypeScript#44360) and the three fix patterns are documented in `docs/TYPING.md`. All 400 services/api + 70 contracts + 47 core + 4 beerjson vitest tests still pass. **Remaining slices — Phase 6h (optional `verbatimModuleSyntax`/`isolatedModules` audit) and docs (public-flip-quality module READMEs) — remain on the H1 2027 timeline.** See [`docs/LINTING.md`](LINTING.md) for the full ESLint phase log and [`docs/TAMAGUI.md`](TAMAGUI.md) for the Tamagui-specific adaptation strategy. The intent stands: a contributor reading the codebase for the first time sees a clean lint surface, descriptive docs about known type-system caveats, and CI gates that prevent regression.

### H2 2027 — WMS as second native-mandatory vertical; federation decision

Goal: validate the "modules expand by config and SDK, not by core rewrite" promise, and decide native packaging strategy with evidence.

- Spike Re.Pack module federation against the platform shell (expecting roughly 30 months of post-MF-release tooling hardening by this point). Decision gate: if the Expo + Re.Pack story is genuinely smooth, federate WMS as the second native module in the same shell; if not, ship WMS as web + PWA + a thin native scanner companion. Either way, the AI consultant sees both modules.
- Brewery + WMS overlap on tooling, packaging, and AI tool registry — the strongest test of whether the platform shape genuinely supports multi-module operation or whether brewery still leaks across boundaries.
- First third-party-built vertical configuration accepted (likely distillery or kombucha) — the proof that the SDK is a public contract, not a private convention.

### 2028 and beyond — CRM, additional vertical configurations, foundation question

- CRM module: native-shipped only if customer ICP demand justifies it; otherwise web-only with PWA on mobile. The decision depends on the segment we actually win, not on hopes.
- Additional process-manufacturing vertical configurations onboarded primarily through **configuration and seed data**, not code (food, cosmetics, supplements, fragrance, fine chemical batch).
- Ecommerce surfaces (if pursued) are explicitly **separate apps** — different audience (shopper vs operator), different ASO, different auth model. Not added to the workspace-member app shell. Out of scope for this roadmap.
- Foundation transfer question (`docs/PLATFORM-ARCHITECTURE.md` §10.1) reopened with evidence: by this point the project either has a community large enough that foundation governance is a meaningful upgrade, or it does not, and the decision becomes much easier than speculating about it now.

### Standing principles across the whole trajectory

- **Web-first for the heavy desktop workflows.** Native apps exist only where workflows are intrinsically mobile (offline operation, BLE, scanning, push notifications, on-the-floor input).
- **One audience per app.** Workspace-member modules share one shell. Shopper-facing surfaces (if any) are separate apps.
- **AI consultant is the cross-module connective tissue.** The architectural and economic case for one shell over a "myriad of apps" rests on the AI seeing all modules in one workspace context.
- **No retroactive license changes.** Anything committed under AGPLv3 stays AGPLv3 (`docs/LICENSING.md` §9–10).
- **Invest in foundations alongside features, not at the end.** Lint, type safety, test coverage, and developer-facing documentation compound — every bit of foundation paid down now makes every future feature cheaper and safer. The opposite — accumulating debt until "later" — never goes well. Living foundation docs: `docs/LINTING.md`, `docs/TAMAGUI.md`, `docs/TESTING.md`, `docs/DEVELOPMENT-ACCESSIBILITY.md`.
- **The tone of this roadmap is honest commitment, not aspiration.** If a phase moves, the doc moves. If the underlying assumptions change, the trajectory changes openly via the same RFC process used for governance changes (`docs/LICENSING.md` §10).

## UI pillars (from Figma)

Figma exports live under `docs/figma/`:
- `dashboard.png`
- `edit-recipe.png`
- `water-calculator-and-mash-chemistry.png`

These map to three “pillars”:
1) Dashboard with simple navigation
2) Recipe editor (complex)
3) Water calculator / mash chemistry (complex)

## Agreed UI/UX decisions (important)

### Navigation (web)
- **Primary nav only** (no extra row of big buttons).
- **Dashboard** is the first/left-most nav item.
- Mobile-friendly by default (nav collapses later, but IA should not change).

### Recipe editing (v0)
- Single edit route with a left-side section list (in-page nav).
- Sections (initial):
  - Basics
  - Fermentables
  - Hops
  - Yeast (may start stubbed)
  - Other ingredients
  - Notes
  - Water chemistry (link-out)

### Water calculator
- Water calculator has its **own page** and is considered part of the recipe.
- The recipe editor should **not** embed the full water calculator; water chemistry in recipe edit is a link to the full calculator.
- UI preference: **dark grey background** (Cursor-like) is desired for recipe + water calculator UIs, **as long as** accessibility constraints are met.

### Recipe import/export (v1)
- Import/export actions live under **Recipes** (not the Dashboard).
- Import UX is split into:
  - **Import single recipe**: user selects a style (default Custom).
  - **Bulk import**: multi-recipe files; style is auto-matched to **BJCP 2021** (name-first, then code), else Custom.
- Export uses **strict BeerJSON** for interoperability (internal addition row `id` fields are stripped).

### Offline-forward constraint (future)
Even though v0 is server-backed, we want to design pages so we can later support offline drafts:
- Each page can maintain a clear “draft saved” model (local-first draft state, explicit save events).
- Avoid duplicated editable sources of truth (aligns with the architecture doc).

## Data prerequisites: seedable raw materials DB

To make recipes and water chemistry usable, we need canonical datasets for:
- Fermentables (malts, sugars, extracts, adjuncts)
- Hops
- Yeast (soon)
- Salts + acids for water correction (curated, small set)
- Water profiles (optional seed)

Approach (agreed direction):
- Start with **BeerProto dataset (MIT)** as the base seed where applicable.
- Preserve provenance for all imported records (source name/url/license/retrieved_at/source_key/raw payload).
- Use a crosswalk table (`ingredient_source_map`) so we can enrich from other sources later without losing traceability.
- Treat non-clear licensing sources as reference-only until confirmed.

