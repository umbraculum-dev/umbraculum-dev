# Optional — production DocSearch on docs.umbraculum.dev (Cloudflare Workers Builds env).
# Copy to docs-site/.env.local for local Algolia smoke (never commit .env.local).
# Search-only API key is public in the client bundle by DocSearch design.
DOCSEARCH_APP_ID=
DOCSEARCH_API_KEY=
DOCSEARCH_INDEX_NAME=
